# ============================================================
# PicoAide Harness 服务端部署配置样例
# 用法: cp .env.example .env 然后修改必填项(务必改密码!)
# 也可由 scripts/deploy.sh install 自动生成(推荐)
# ============================================================
# 只有以下 4 个键需要配置,其余全部在 docker-compose.yml 内置默认值
# (SERVER_IMAGE 可选,见文末)。

# ---- 必填 ----
# 对外域名或 IP(访问地址,如 picoaide.example.com 或 10.0.0.5)
DOMAIN=picoaide.example.com

# 初始管理员密码(首次启动创建 admin;已有 admin 后幂等跳过,可置空清除明文)
PICOAI_ADMIN_PASSWORD=change-me-to-a-strong-password

# 内置 PostgreSQL 容器密码(首次部署时创建;请改成强密码)
PG_PASSWORD=change-me-to-a-strong-password

# 证书模式(三选一,重要):
#   manual   = 手动证书: 使用 certs/server.crt + certs/server.key(缺省由部署脚本/openssl 生成自签占位,
#              正式证书替换:覆盖 certs/ 下两文件 → docker compose restart caddy)
#   auto     = 自动证书: Caddy 自动申请/续期 Let's Encrypt(需公网域名直连本机,80/443 对外开放;
#              域名经 CDN 时安装脚本会要求人工确认,或直接选用 manual)
#   internal = 本地自签: Caddy 本地 CA 签发(tls internal),纯内网/无公网域名开箱即用
TLS_MODE=manual

# ---- 可选(均有默认值,一般不用改) ----
# 服务端镜像标签。镜像来自更新服务器(下载 zip → docker load),不经过任何
# 镜像仓库;只有本地构建时用 make docker-image 自己打标。
# 升级时按部署说明改成目标版本。官方发布包里的镜像**两种 tag 都带**
# (2.7.0 与 v2.7.0),写哪个都能起来;清单 latest.json 的 server.image_tag 是
# 带 v 的形式,以它为准。
# SERVER_IMAGE=picoaide-harness-server:2.7.0

# 私有网段与容器固定 IP(与宿主机其他网段冲突时改这里)
# NETWORK_SUBNET=172.28.0.0/24
# CADDY_IP=172.28.0.2
# SERVER_IP=172.28.0.3
# PG_IP=172.28.0.4

# 可信反向代理(审计 2026-08-25 F-02):登录限流据此解析真实客户端 IP,
# 防反代后限流键坍缩为单一代理 IP(单账号 DoS);改网段时应同步 CADDY_IP。
# 本地直连(无 Caddy)可留空。
# PICOAI_TRUSTED_PROXIES=172.28.0.2

# ---- WASM 应用平台（员工自建应用，设计基线 docs/planning/2026-09-17-wasm-app-platform.md）----
#
# 应用基域：应用通过 `<app_id>.<应用基域>` 访问（app_id 本身即域名标签）。
# **留空 = 不启用应用子域**（此时只有发布/校验链路可用，应用无法被访问）；
# 启用时必须同时：①企业自备该基域的通配证书（R29，平台不做证书自动化）
# ②显式配置 PICOAI_TRUSTED_PROXIES（见下，未显式配置会**拒绝启动**）
# ③在 Caddy 里为该基域配 `*.<应用基域>` 的站点块。
#
# **基域必须与下面的对外地址（PICOAI_PUBLIC_BASE_URL）同域**，且必须是能承载
# Cookie 的普通域名（不能是 IP 地址，也不能含下划线）：换票的 nonce Cookie 要写
# `Domain=<基域>`，写不出去（或与对外地址不同域）时平台会**拒绝签发票**（登录可见 /
# 白名单应用一律 500，日志给出可执行动作）——**没有**"允许无 nonce 换票"的运维开关。
# 本示例与下面的对外地址示例一致（基域 = 主站主机，应用是它的子域）。
# PICOAI_APPS_BASE_DOMAIN=harness.example.com
#
# 部署期注入的**企业既有主机名**（逗号分隔）：基域是平台资产，应用不得占用
# 这些名字（如 intranet、oa、mail）。命中即拒绝发布（§4.1）。
# PICOAI_APPS_EXTRA_RESERVED=intranet,oa
#
# 编译子进程的 OS 级隔离档（R31）：
#   require = 隔离不可用就拒绝启动（**生产推荐**）
#   auto    = 可用则启用，不可用则大声记日志后继续（默认）
#   off     = 关闭隔离（**仅测试**，会大声记日志）
# 容器里 bubblewrap 需要非特权 user namespace 或给容器 `--cap-add SYS_ADMIN`。
# PICOAI_COMPILE_ISOLATION=require
#
# 内置技能资产目录（"技能内置到服务端、客户端按需安装"）：
# 技能随镜像发布在 /opt/picoaide/skills（当前是 WASM 应用作者手册
# app-builder）。服务端由这两个端点下发，客户端在能力中心点安装：
#   GET /api/client/v2/skills/builtin
#   GET /api/client/v2/skills/builtin/<name>/archive
# **默认值就是镜像内的位置，通常不需要配置**；只有要把资产换成宿主机目录
# （现场排查、临时补一个技能）时才改，并在 docker-compose.yml 里同时解开
# `./skills:/opt/picoaide/skills:ro` 那条挂载。
# PICOAI_SKILL_SEED_DIR=/opt/picoaide/skills

# Caddy 宿主机端口映射(改端口时需同步修改 Caddyfile;一般不用动)
# CADDY_HTTP_PORT=80
# CADDY_HTTPS_PORT=443

# 时区
# TZ=Asia/Shanghai

# ---- 渠道(一般不用填) ----
# 本部署所属渠道。**留空即可**:渠道由镜像自带的声明决定,镜像里的渠道内容
# (channels/<id>/channel.json)与它必须一致,不一致服务端拒绝启动。
# 只有确实要覆盖镜像声明时才填;填了就必须与镜像内的渠道一致。
# PICOAI_CHANNEL=official

# 更新清单地址。留空 = 用本渠道的默认目录
# (release.picoaide.com/<channel>/latest.json),管理员可在 webadmin 系统信息页
# 看到"有新版本"提示。要**关闭**更新检查请显式写 off(留空不等于关闭)。
# PICOAI_UPDATE_ENDPOINT=https://release.picoaide.com/official/latest.json

# 本服务端对外可达地址(如 https://harness.example.com)。**配了就是唯一权威**,
# 留空则按请求推导(X-Forwarded-Proto: https → TLS → 回环 http)。
#
# 为什么建议配:客户端更新清单里的下载地址必须是**绝对 https**(客户端会整份丢弃
# 非 https 的清单)。若前面挂的是不设 X-Forwarded-Proto 的反代、或服务端判断不出
# 协议,清单会按设计拒发 client 段(`client_unavailable`)——用户看到的是"永远已是最新"。
# **宿主机已有共享反代(共享 Caddy/nginx)时必须配**(见部署文档附录 A)。
#
# ⚠️ 启用 WASM 应用子域(`PICOAI_APPS_BASE_DOMAIN` 非空)时,本地址的**主机名必须与
# 应用基域相同**(示例:两者都是 harness.example.com —— 主站是基域本身,应用是
# `<app_id>.harness.example.com`)。配成另一个域不会"降级可用",而是让登录可见/白名单
# 应用**一律拒绝签发票**(换票 nonce Cookie 无法下发;2026-09-19 第二轮审计 §1.3 的
# 现场),且**没有**运维开关可以放开。
# PICOAI_PUBLIC_BASE_URL=https://harness.example.com

# 单账号登录失败上限(留空 = 服务端默认)。仅**测试环境**建议调大,
# 免得反复登录把管理员账号锁死。
# PICOAI_LOGIN_MAX_ATTEMPTS=100000

# 主密钥(可选,强烈建议备份):留空则服务端首次启动自动生成 data/master.key
# PICOAI_MASTER_KEY=
