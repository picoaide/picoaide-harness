# Cron scheduler Host rows: the bare entry carries the package's client
# bundle declaration (job center + settings card) and provides the
# `picoCronService` Host service consumed by sibling plugins (e.g. dsh-task
# registers per-task scheduled runs through it).
- insert:
    - id: pico-cron
      name: '@picoaide/dsh-cron'
