# Connector framework Host rows: the framework plugin owns the connector
# registry, auth orchestration and MCP registration; its client half provides
# the settings「连接器」section.
- insert:
    - id: pico-connectors
      name: '@picoaide/dsh-connectors'
