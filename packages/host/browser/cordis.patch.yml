# dsh-browser Host rows: the embedded agent-driven browser owns its
# WebContentsView tabs, CDP driving, tool registration and the loopback
# panel API consumed by the client browser panel.
- insert:
    - id: pico-browser
      name: '@picoaide/dsh-browser'
