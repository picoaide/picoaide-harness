/** Vendored acorn 8.15.0 (MIT) — UMD build loaded as CJS. */
declare const acorn: {
  parse(input: string, options?: { ecmaVersion?: number; locations?: boolean; sourceType?: 'script' | 'module' }): unknown
}
export = acorn
