# Account card row: the bottom sidebar account card (username + logout +
# live usage balance). Loaded after the enterprise rows — the service it
# injects (`picoSession`) is provided by picoaide-session.
- insert:
    - id: picoaide-account-card
      name: '@picoaide/dsh-account-card'
