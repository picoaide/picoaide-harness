发布于 2026-08-28。本版本完成品牌版本号修复与网关 provider 双端点协议，并加固 Docker 镜像验证。

## 品牌

- **修复版本号未生效问题**：版本号显示在真正生效的 enterprise 品牌源（branding 包此前未被打包进安装包）

## 网关

- **provider 协议 both**：同一 key 双端点（chat + search），webadmin 零额外配置

## CI / 部署

- Docker 镜像验证需 PostgreSQL：healthz ping DB，起 postgres sidecar 传 `-pg-dsn`
- dist:win/mac 预构建调整：保持 dist 脚本表面不变、构建顺序 desktop 最先（enterprise tsc 依赖 desktop 的 lib/types）
