# 多语言（i18n）覆盖审计 — 2026-09-16

**审计对象**：本仓库全部自研插件与内容面（`packages/host/*`、`packages/client/*`、`packages/vendor/memory-evolve`、`server/webadmin`、`server/internal/**` 的界面文案）
**基线**：`chore/release-v2.7.5-beta.1` @ `3327104e0e`（上游 pin = dsh 0.1.5-rc.2）
**性质**：只读审计，未改动任何受版本控制的文件
**方法**：TypeScript 编译器 API 遍历 AST（非 grep），只统计真实字符串字面量 / JSX 文本节点，按消费点逐个判定是否用户可见；注释、CSS 类名、import 路径、测试文件全部排除。四条独立子审计交叉复核。

---

## 0. 结论摘要

**机制是对的，覆盖是坏的。**

已有的 i18n 机制（`locales.ts` + `ctx.locale` 订阅）**工作正常**：语言切换会实时生效，字典本身健康（键完全对齐、无 `en` 列残留中文）。问题不在机制，而在于**大量界面根本没有接进这套机制**——尤其是弹窗、预认证页面、内置浏览器窗口、管理后台。

| 面 | 用户可见未翻译字符串 | i18n 基础设施 | 严重度 |
|---|---|---|---|
| **webadmin 管理后台** | **~1,455 处 / 1,301 条**（含 29 个 Radix 弹窗 + 17 处原生 confirm） | **完全没有** | 🔴 P0 |
| **memory-evolve**（画板 / Advisor / CoI / Prompt） | **313 条**（其中画板弹窗 58、Advisor 面板 131） | 有字典（795 键）但**被绕过** | 🔴 P0 |
| **预认证页面**（登录 / 改密 / 恢复） | **~44 条**，`lang="zh-CN"` 硬编码 | **完全没有** | 🔴 P0 |
| **内置浏览器窗口**（工具栏 + 蒙版 + 活动面板） | **~85 条**，`lang="zh-CN"` 硬编码 | **完全没有** | 🔴 P0 |
| **服务端 Go 错误文案** | **722 条** `WriteError` 消息直通前端 | **完全没有**（无 `Accept-Language`） | 🟠 P1 |
| **门户页**（`/`、`/portal`） | 整页中文，`lang="zh-CN"` | **完全没有** | 🟠 P1 |
| **desktop 客户端 + branding** | 4 条（含 1 处中英混排按钮） | **插件未注入 `locale`** | 🟡 P2 |
| connectors / enterprise / browser / cron / account-card | 错误载荷 ~90 条（壳已本地化） | 有字典，机制正确 | 🟡 P2 |

**一句话**：所有「我们自己手写的 HTML 页面」和「webadmin」是零覆盖；memory-evolve 是「有字典但一半绕过」；其余插件是「壳已本地化、载荷未本地化」。

---

## 0.5 【P0 独立缺陷】macOS 初次语言判定恒为英文 —— 打包配置少了一个下划线

**症状**（用户 2026-09-16 报告）：macOS 上首次启动恒为英文，**系统语言是中文也一样**；托盘与界面同时中招。手动在「设置 → 通用 → 语言」切换后能生效（因为那会写入 `locale.preference`），但每次全新安装/未设置过偏好的用户都从英文开始。

### 根因

`packages/host/desktop/package.json:313-316`：

```json
"electronLanguages": [
  "zh-CN",
  "en-US"
],
```

electron-builder 的 `electronLanguages` 用来**裁剪 Electron 的本地化目录**，而 macOS 上这些目录是 **`.lproj` 目录**，命名用**下划线**（Chromium 的 locale id 用连字符，macOS bundle 约定用下划线）。Electron 43.4.0 的 mac 包在主 bundle 的 `Contents/Resources/` 下带 **57 个 `.lproj`**，其中中文是 **`zh_CN.lproj`** / `zh_TW.lproj`（实测：读 `electron-v43.4.0-darwin-arm64.zip` 的 zip central directory）。

electron-builder 26.15.3 的匹配逻辑（`app-builder-lib/out/electron/ElectronFramework.js:67-90`）是**字面量比较**，比较对象是目录 basename 的小写形式：

```js
const language = path.basename(file, ".lproj").toLowerCase()   // zh_CN.lproj → "zh_cn"
const isWantedLocale = wantedLanguages.some(wantedLanguage =>
  wantedLanguage === language                     // 精确
  || wantedLanguage.startsWith(`${language}-`)    // 前缀（连字符）
  || wantedLanguage.startsWith(`${language}_`)    // 前缀（下划线）
)
```

代入我们的配置（已小写化）：

| 配置项 | 目录 basename | 判定 | 结果 |
|---|---|---|---|
| `"zh-cn"` | `zh_cn` | `"zh-cn" === "zh_cn"` ✗；`.startsWith("zh_cn-")` ✗；`.startsWith("zh_cn_")` ✗ | **删除** ❌ |
| `"en-us"` | `en` | `"en-us".startsWith("en-")` ✓ | 保留 ✅ |

**连字符 vs 下划线之差 ⇒ `zh_CN.lproj` 被删掉，只剩 `en.lproj`。**

实测产物佐证（本分支刚打出的 `temp/mac-tray/beta-2.7.5-beta.1-mac.dmg`）：

```
PicoAide Harness.app/Contents/Resources/
  en.lproj          ← 唯一幸存，且是空目录
  （其余 56 个 .lproj 全部被删）
```

### 为什么这会导致「界面也是英文」而不只是托盘

macOS 解析 App 语言的规则是：**用户的首选语言列表 ∩ App 声明支持的本地化**。App 只声明了 `en`，中文系统下交集为空 ⇒ 回落到开发区域（默认 `en`）。于是：

```
系统语言 = 简体中文
  → macOS 判定 App 语言 = en（因为 bundle 只声明 en）
  → app.getLocale()        = "en-US"        → 托盘走 desktopLocaleFromLanguageTag → 'en'
  → navigator.languages    = ["en-US","en"] → upstream detectBrowserLocale 命中 'en' → 界面英文
```

一处根因、两个症状。与上游 `deepseek-harness/packages/client/locale/src/client/index.ts:515-521` 的 `detectBrowserLocale` 无关——**那段代码本身是对的**（本机实测：`LANG=zh_CN.UTF-8` 时 `navigator.languages` 正确返回 `["zh-CN","zh"]`，`app.getLocale()` 返回 `zh-CN`）。是喂给它的输入在 macOS 打包时被污染了。

同类已知问题：Electron [#26612](https://github.com/electron/electron/issues/26612)、lobehub [PR #17804](https://github.com/lobehub/lobehub/pull/17804)（标题即 *"resolve UI language from the OS instead of the pruned app locale"*，其提交说明写明打包裁剪 `.lproj` 会让 `app.getLocale()` 与 `navigator.language` 双双报 `en-US`）。

### 修复（已实施 2026-09-16，commit `afaea04346`）

**两种平台的拼写都要写进同一个列表**：

```json
"electronLanguages": [
  "zh-CN",
  "zh_CN",
  "en-US"
],
```

> ### ⚠️ 本报告的初版修复建议是错的，此处更正
>
> 本审计最初只验证了 **macOS** 一条路径，给出的方案是 `["en-US","zh_CN"]`。**那会弄坏 Linux/Windows 的中文界面** —— `electronLanguages` 同时裁剪两个平台的资源，而两边的资源基名拼写不同：
>
> | 平台 | 资源 | 基名拼写 |
> |---|---|---|
> | Linux / Windows | `locales/*.pak`（Chromium locale id） | **连字符** `zh-CN.pak` |
> | macOS | `Contents/Resources/*.lproj`（bundle 约定） | **下划线** `zh_CN.lproj` |
>
> 反向匹配是 `wanted === base` 或 `wanted.startsWith(base + '-' | '_')`，于是单写任一种都会让另一个平台的中文整片消失。实测（`afaea04346` 提交说明）：写 `["en-US","zh_CN"]` 打包后 `dist/linux-unpacked/locales/` 只剩 `en-US.pak`，`e2e:client` 的中文断言连带 8 项变红。
>
> **教训**：验证一个跨平台打包开关时，必须把**每个平台**的资源基名都代入匹配规则跑一遍，不能只测出问题的那一个平台。本报告的教训已固化为守卫。

用 electron-builder 的真实匹配逻辑 + Electron 43.4.0 两个平台的真实资源名对拍后的结论：

| 配置 | macOS 幸存 `.lproj` | Linux/Windows 幸存 `.pak` | 中文 |
|---|---|---|---|
| `["zh-CN","en-US"]`（原始） | `en.lproj` | `en-US.pak`, `zh-CN.pak` | macOS ❌ / Linux ✅ |
| `["en-US","zh_CN"]`（**错误建议**） | `en.lproj`, `zh_CN.lproj` | `en-US.pak` | macOS ✅ / Linux ❌ |
| `["zh-CN","zh_CN","en-US"]`（**现行**） | `en.lproj`, `zh_CN.lproj` | `en-US.pak`, `zh-CN.pak` | **两边都 ✅** |

**回归守卫已落地**：`packages/host/desktop/tests/electron-languages.spec.ts` —— 按 electron-builder 的反向匹配规则，对两个平台各自的真实资源基名分别断言「必须被命中」，并单独断言中文两种拼写都在列表里；已自证正反（改回单拼写时 2 条断言变红）。离线判据脚本仍在 `temp/i18n-audit/verify-electronlanguages.py`（仅覆盖 macOS 路径，**勿**单独用它下结论）。

`en-US` 同时命中 macOS 的 `en.lproj`（靠 `startsWith('en-')` 那条分支）。
（未含 `zh_TW`：用户 2026-09-16 定案只要简中 + 英文；将来若需繁体，按同一规则把 `zh-TW` 与 `zh_TW` **两种拼写都加**。）

**B（可选加固，未实施）** —— 显式声明支持的本地化，不依赖 `.lproj` 扫描结果：

```json
"mac": {
  "extendInfo": {
    "CFBundleLocalizations": ["en", "zh-Hans", "zh-Hant"]
  }
}
```

**C（可选，未实施）** —— 主进程用 `app.getPreferredSystemLanguages()`（读系统设置、**不受 App 本地化裁剪影响**）解析语言，再喂给渲染进程，使任何代码路径都不再依赖被污染的值。lobehub 走的就是这条路（经 `additionalArguments` + preload 传给渲染进程）。

> 注意：主进程已有 `showDirectoryPicker` 在用 `this.currentLocale`（`electron-runtime.ts:262`），A 修好后 `app.getLocale()` 即恢复正常，托盘、原生对话框、渲染进程一并转正。

### 为什么 CI 一直没发现

`packages/host/desktop/scripts/e2e-client.mjs:221` 恒定用 `--lang=zh-CN` 启动被测应用，**强行覆盖了被污染的语言判定**，断言又全是中文 marker。也就是说这条链路在 CI 里从来没有按用户真实路径跑过（本报告 §6 S8）。

### 验证建议

本机无法复现 macOS 的 bundle 语言解析，需在真机验证：

1. 在**系统语言为简体中文**且**从未设置过 App 内语言偏好**的 macOS 上装新包（旧 `~/.picoaide-harness` 里的 `locale.preference` 会掩盖问题，需先删或换新用户）；
2. 首启应直接中文（界面 + 托盘）；
3. 离线判据：`/Applications/PicoAide Harness.app/Contents/Resources/` 下应同时存在 `en.lproj` 与 `zh_CN.lproj`（当前只有前者）；
4. 反向用例：系统语言英文时首启仍应为英文。

---

## 1. 机制现状（先说好的部分）

### 1.1 已有字典的包，机制是正确的

五个包各自维护 `src/client/locales.ts`：

```ts
export const zh = { 'key': '文案' }                       // zh 是键源
export const en: Record<keyof typeof zh, string> = {...}  // 编译期强制键对齐
let activeLocale: 'zh' | 'en' = 'zh'
export function setActiveLocale(id: string): void {...}
export function t(key, params?): string {...}             // 渲染期取值 + {name} 插值
```

并在 client 入口跟随宿主语言：

```ts
ctx.effect(() => {
  const locale = ctx.locale as unknown as { getLocale?(): {active?: unknown}; subscribe?(fn): () => void }
  const sync = () => { const a = locale.getLocale?.()?.active; if (typeof a === 'string') setActiveLocale(a) }
  sync()
  return typeof locale.subscribe === 'function' ? locale.subscribe(sync) : () => {}
}, 'follow active locale')
```

**关键验证：运行时切换确实生效。** 上游 `ui-renderer/src/client/scoped-slots.tsx:280` 明确写着 *"Every outlet re-renders on a locale switch"*，`:707` 每个 outlet 订阅 locale revision。因此槽位里注册的组件会重渲染，模块级 `t()` 在渲染期重新取值 → 语言即时跟随。五个包的 `index.ts` 都做了 `subscribe`，**不是 apply 时读一次**。

`memory-evolve` 走的是另一条同样正确的路：`ctx.locale.bind(NS)` 在调用期解析语言。

### 1.2 字典健康度：全部通过

| 包 | zh 键 | en 键 | 键对齐 | en 列残留中文 |
|---|---|---|---|---|
| connectors | 50 | 50 | ✅ | 0 |
| cron | 55 | 55 | ✅ | 0 |
| enterprise | 171 | 171 | ✅ | 0 |
| browser | 3 | 3 | ✅ | **1** ⚠️ |
| account-card | 13 | 13 | ✅ | 0 |
| memory-evolve | 795 | 795 | ✅ | 0 |

唯一的 `en` 残留：`packages/host/browser/src/client/locales.ts:14`

```
'panel.waiting': 'The AI is waiting for you to hand back browser control: open the browser window and click 交给 AI to continue'
```

英文句子里嵌了未翻译的按钮名「交给 AI」。应改为 `{button}` 占位符——但 browser 的 `t()` 是五个包里**唯一不接受 `params`** 的（`t(key: BrowserKey): string`），需要先补齐签名。

---

## 2. P0-A：弹窗（用户点名的问题）

### 2.1 memory-evolve 画板弹窗 — 6 个弹窗，100% 硬编码中文

`packages/vendor/memory-evolve/src/client/canvas-grok/CanvasDialogs.tsx`（58 条）：

| 弹窗 | 位置 | 硬编码文案（节选） |
|---|---|---|
| **路径上板** `PathDialog` | `:46-83` | `aria-label="路径上板"` · `<h3>路径上板</h3>` · `粘贴本地路径即可生成卡片。暂不校验文件是否存在，卡片会标「未验证」。` · `本地路径` · placeholder `/Users/me/Documents/合同.pdf` · `将识别为：` · `取消` / `上板` |
| **便签上板** `NoteDialog` | `:85-124` | `未命名便签` · `aria-label="新建便签"` · `便签上板` · `内容存在画板里，不指向任何文件。` · `标题` / `类型` / `内容` · `纯文本` · `取消` / `上板` |
| **搜索上板** `CatalogDialog` | `:126-233` | `搜索上板` · `搜索本机文件，选中即上板。` · `关键字` · `合同 / 设计稿 / 录音…` · `搜索` · `搜索范围` · `本机全部` / `当前项目` · `搜索中…` · `没有匹配的文件` · `本地搜索不可用` · `关闭` |
| **预览** `PreviewDialog` | `:235-313` | `预览` · `画板内便签` · ` · 路径未验证` · `（空内容）` · `启用画板模块后可预览真实图片` · `用默认应用打开` · `关闭` |
| **确认移除** `RemoveDialog` | `:315-340` | `确认移除` · `从画板移除？` · `将移除「{title}」。只从画板拿掉，不删除源文件` · `取消` / `移除` |
| **迁移归属** `MigrateDialog` | `:342-372` | `迁移归属` · `「{title}」将移动到：` · `💬 本会话` / `📁 本项目` / `🌐 所有项目可见` · `取消` / `迁移` |

**讽刺的是 `t` 早就传到位了。** 调用链：`src/client/index.ts:2444` → `registerCanvasTab(ctx, { t, openSession })` → `canvas-grok/index.ts:78` 转发给 `CanvasView` → **`CanvasView.tsx:62` 声明了 `t: Translate` 却从未读取**（整个子树零 `props.t` 命中）。所以修法不是「接上管道」，而是「把已经在那儿的 `t` 用起来」。

### 2.2 memory-evolve Advisor 悬浮面板 — 131 条

`advisor/AdvisorPanel.tsx`（149 处字面量，131 条无英文路径）是**单文件最大缺口**：

- 状态/严重度标签 `:61-91`（`已停用`/`空闲`/`评审中`/`已暂停`/`已终止`、`info · 记录`/`nit · 建议`/`concern · 关注`/`blocker · 阻断`）
- 时间助手 `:125-130`（`暂无活动`/`刚刚`/`{n} 秒前`/`{n} 分钟前`/`{n} 小时前`）
- **四个 Tab** `:569,578,587,597`（`约束`/`实时`/`记录`/`设置`）
- 实时流 `:608-721`，含 **`:676` 原生 `window.confirm('新建评审会话将清空评审员的全部上下文与记忆…')`**
- 历史 / 约束 / 设置三大区 `:730-1221`

`t` 同样已经传入（`src/client/index.ts:2176`），组件只在 2 处用了它（`:325`、`:380`）。文件头注释写着「面板主体按需求固定中文」——**这是一个需要产品拍板的既定选择，不是遗漏**。另有 18 处用手写 `isEn()`（读 `navigator.language`）而非 `ctx.locale`，属于第三套并存的语言判断。

### 2.3 webadmin — 29 个弹窗 + 17 处原生对话框，全部中文

管理后台是一个**独立 Vite/React 应用**（`base: '/admin/'`），不是 DSH client 插件，**没有 `ctx.locale` 可用**，且当前**零 i18n 基础设施**（无库、无字典、无 provider、无 `t()`）。

弹窗清单（全部硬编码）：

| 类型 | 数量 | 位置 |
|---|---|---|
| Radix `<Dialog>` 模态 | **29** | `password-dialog`(强制改密) · `mfa-settings-dialog` · `grant-dialog` · `transfer-owner-dialog` · `archive-preview-dialog` · `capability-lock-panel` · `Connectors.tsx` ×2 · `Agents.tsx` ×3 · `Marketplace.tsx` ×3 · `Capabilities.tsx` ×1 · `Users.tsx` ×5 · `Gateway.tsx` ×5 · `Departments.tsx` ×1 · `usage/Balance.tsx` ×2 · `usage/Reports.tsx` ×1 |
| 原生 `window.confirm` / `alert` | **17** | `Users.tsx:161,178,180,214,335` · `Gateway.tsx:400,419` · `Marketplace.tsx:219,263,333,375` + `alert` `:271` · `Departments.tsx:97` · `usage/Reports.tsx:97` · `grant-dialog.tsx:65,104` · `capability-lock-panel.tsx:50` |
| Select / Tabs / Popover | 11 个文件 | 全部中文选项 |

**风险最高的是破坏性确认**：删除用户/部门/连接器、清空高峰时段配置、通过/拒绝上传、重置密码、开关 MFA——非中文管理员有误操作风险。

原生 `confirm()` 还有个**已经可见的混排缺陷**：其 OK/Cancel 按钮跟随*浏览器*语言，正文是中文，所以在英文浏览器上现在就是中英混排。

其余 webadmin 数据：39 个非测试源文件含用户可见中文；23 个测试文件也是纯中文断言（631 处），翻译会连带打挂测试；`index.html` 硬编码 `lang="zh-CN"` 与 `<title>管理后台</title>`（`dist/` 产物一致）。

---

## 3. P0-B：预认证页面与内置浏览器窗口

这两处都是**我们自己手写的 HTML 页面**（不是 React 组件），因此既拿不到 `t()`，也拿不到 `ctx.locale`（`ctx.locale` 只存在于 client face）。

### 3.1 登录页 — 全产品第一个界面，100% 中文

`packages/host/enterprise/src/auth-gate.ts`，四段 HTML 全部 `lang="zh-CN"`，**文件内零 locale 输入**（只替换 `__BRAND_NAME__` / `__DEFAULT_SERVER__` / `__BRAND_JSON__` / `__BACK_BUTTON__`）：

| 文档 | 行 | 字符串 | 内容 |
|---|---|---|---|
| `LOGIN_HTML` | `:52-612` | ~29 | `连接服务端` · `输入服务端地址以确认登录方式` · `下一步` · `账号`/`密码` placeholder · `登录` · `使用浏览器登录` · `请在弹出的浏览器窗口中完成授权…` · `账号或密码错误` · `登录尝试过于频繁，请稍后再试` · `账号已被禁用，请联系管理员` · `审计账号不可登录客户端，请使用管理后台` |
| `CHANGE_PASSWORD_HTML` | `:613-685` | 14 | `修改密码` · `你的密码已被管理员重置…` · `当前密码(管理员设置的临时密码)` · `新密码(至少 10 位)` · `两次输入的新密码不一致` · `提交中…` |
| `RESTORING_HTML` | `:686` | 1 | `正在恢复登录状态…` |
| `BACK_BUTTON_HTML` | `:828` | 1 | `← 修改服务端地址` |

### 3.2 内置浏览器窗口 — 整个浏览器 chrome，约 85 条

`packages/host/browser/src/shell-pages.ts`：**该文件零 import**，是纯静态模板模块，通过 `webServer` exact 路由 `/browser-shell`、`/browser-overlay` 喂给 Electron `BrowserWindow` + `WebContentsView`。

`BROWSER_SHELL_HTML`（`:17`，工具栏页）与 `BROWSER_OVERLAY_HTML`（`:317`，蒙版 / 活动面板 / ⋮ 菜单 / 查看器）合计约 85 条用户可见字符串，全部中文：

- 工具栏：`AI 浏览器`(title) · `新建标签页 (Ctrl+T)` · `后退 (Alt+←)` · `前进 (Alt+→)` · `刷新 (Ctrl+R)` · `输入网址，回车访问（例如 https://example.com）` · `地址栏`(aria) · `收藏到书签` · `更多`
- 空态：`打开浏览器，AI 会在需要时自动打开网页。你也可以点右上角 ＋ 先自己逛起来。`
- 控制权按钮：`我来操作`（4 处渲染点）· `交给 AI` · `AI 正在操作` · `你正在操作` · `暂停 AI，自己操作`
- **29 条动作时间线标签**：`打开标签页`/`打开网页`/`刷新页面`/`后退`/`前进`/`点击`/`输入文字`/`按键`/`选择下拉项`/`滚动页面`/`截图`/`读取页面元素`/…/`关闭浏览器`
- 菜单与查看器：`浏览历史`/`书签`/`下载`/`清除数据…`/`关闭` · `清除全部浏览数据（含登录状态）？`(confirm) · `暂无记录`/`加载失败，请重试`
- toast：`操作失败：浏览器会话凭据尚未就绪，请重试` 等 5 条

**注意**：内置浏览器是产品的招牌功能之一，目前对英文用户 100% 不可用。

### 3.3 desktop 崩溃兜底页与 Linux 更新对话框

| 位置 | 文案 | 问题 |
|---|---|---|
| `electron-runtime.ts:974` | `界面加载失败` / `渲染进程未能正常加载…` / `重新加载` | 崩溃兜底页整页中文 |
| `electron-runtime.ts:583` | `新版本 AppImage 已下载到: …在界面或托盘里点「安装更新」后…` | **中英混排**：同一对话框的 `title`/`message`/`buttons` 是英文，darwin/win32 分支也是英文，只有 Linux 的 detail 是中文 |
| `electron-runtime.ts:612` | `新版本 AppImage 已下载到: …请关闭本程序…` | 同上 |

对照：`electron-runtime.ts:262` 的原生目录选择器**已经**做了 `this.currentLocale === 'zh' ? '选择工作区目录' : 'Select Workspace Directory'`，`currentLocale` 就在同一个类上——所以这是**漏改**，不是缺管道。

---

## 4. P1：服务端文案与门户页

### 4.1 Go 错误信封 — 722 条中文消息直通界面

`server/internal/**` 的 `serverauth.WriteError` / `writeError` 共 **722 条中文消息**，只归到约 22 个 `code`（`VALIDATION` 一个码下就有 169 条不同文案）。前端 `server/webadmin/src/api.ts:82` 原样取出：

```ts
message = body?.error?.message ?? message
```

**因此「按 `code` 在前端映射」这条路走不通**——同一个码对应上百条不同文案。全仓**无任何 `Accept-Language` 处理**。

另外两类也直通界面：
- **服务端生成的审计 `detail`**：`Audit.tsx:317` 原样渲染 `{l.detail}`（例：`appstore/admin.go:38` 拼的 `" 归属 " + from + " → " + to`）
- **服务端 `fmt.Sprintf` 校验串**（17 处）：如 `serverauth/admin.go:517` `"密码至少 %d 位"`、`archiveutil/archive.go:819` `"归档缺少 %s"`

### 4.2 门户页

`server/internal/portal/portal.go:127` 与 `:418`（错误页）同样硬编码 `lang="zh-CN"`，整页中文（客户端下载、功能列表、数据边界说明）。这是员工拿安装包的第一入口。

---

## 5. P2：插件侧「壳已本地化、载荷未本地化」

这些包的**框架**是对的——但宿主/服务端抛出的错误载荷是中文，英文用户看到的是 `Connection failed: OAuth 授权已取消`。

### 5.1 connectors（90 条经错误包装露出）

链路：宿主 `setState({error})` 或 `json(res,…,{error})` → `ConnectorsSection.tsx:496` 渲染 `friendlyConnectorError(entry.error)` → 未匹配时回落 `t('error.generic', {message: raw})` = `Connection failed: {message}`。**壳是本地化的，载荷不是。**

| 文件 | 条数 | 代表 |
|---|---|---|
| `src/auth.ts` | 26 | `:317` **OAuth 回环回调页** `<p>授权完成，可以关闭此窗口。</p>`（渲染在用户浏览器里）；`:297` `等待授权超时（5 分钟）` |
| `src/policy.ts` | 17 | `:445` `tokenFields 的 key 不被允许: …`（→ 面板 rejected[]） |
| `src/mcp-oauth-provider.ts` | 17 | `:268` `MCP 端点公开可用，无需令牌`；`:352` `refresh token 已失效，需要重新授权` |
| `src/mcp-transport-fence.ts` | 15 | `:727` `MCP streamable-http 传输不可加固: …` |
| `src/outbound.ts` | 9 | `:229` `…指向内网/链路本地/元数据地址，已拒绝: …` |
| `src/index.ts` | 5 | `:1051` `需要先完成授权：当前凭据被服务端拒绝（点击「连接」重新授权）` |
| `src/client/index.ts` | 1 | `:118` `join('、')` → 英文界面里出现 `for example: a、b` |

### 5.2 enterprise 面板与预检

| 位置 | 文案 | 说明 |
|---|---|---|
| `client/CapabilityCenterPanel.tsx:628` | `` `名称已被占用:「${...}」已存在于能力中心,请更换名称或联系管理员` `` | **确认为纯遗漏**：同文件已 import `t` 并用了 74 次，字典里没有对应 key |
| `manifest-precheck.ts` | **23 条** | 走 `skill-install.ts:478` throw → `auth-gate.ts:1802` `json(res,422,{error})` → 面板 `t('capability.failed',{error})`，**确实到用户** |
| `skill-install.ts:461,478` | 2 条 | 同上（`（另有 {n} 项问题）` 还是全角括号） |
| `client/channel-vars.ts:12` | `DEFAULT_HERO_TAGLINE = '企业版'` | **登录后第一屏** hero 徽标 |
| `client/index.ts:76` | `content: var(--pico-hero-tagline, "企业版")` | 同一个徽标的 CSS 兜底——**同一文案两处重复** |
| `server-connector/auth.ts:9-12,100,217,219` | 7 条 | 登录页错误（`账号或密码错误` 等） |

### 5.3 cron 与 browser 的标点/细节

- `cron/src/client/JobEditor.tsx:193,207,217,223,237` — 用**全角括号 `（）` 包裹已本地化的插值**，英文界面文案正确但括号仍是中文标点（排版问题，非未翻译）
- `browser/src/client/locales.ts:14` — 见 §1.2 的 `en` 残留
- `browser/src/runtime.ts:3118` — `clear browsing data (${…})` 英文框架 + 中文取值

### 5.4 desktop 客户端与 branding —— 缺 `locale` 注入

| 位置 | 文案 | 根因 |
|---|---|---|
| `desktop/src/client/desktop-update.tsx:276` | `` label: `安装 ${ready}` `` | **中英混排按钮**：同一对象的 `title` 是 `Version ${ready} is downloaded — click to install`。该插件 `inject = ['slots','sessions','theme']`——**没有 `locale`** |
| `client/branding/src/client/brand-shell.tsx:147` | `PicoAide 品牌与界面壳：…无上游代码分支。` | 设置页「关于」段落（`createElement` 子节点，**用户可见**）。同段其他行是硬编码英文 → 中英混排。该插件 `inject = ['slots','theme']`——**没有 `locale`** |

---

## 6. 结构性缺陷（比漏翻译更值得先修）

| # | 缺陷 | 证据 | 影响 |
|---|---|---|---|
| **S1** | **没有 host 侧翻译服务** | `@deepseek-ai/dsh-client-locale` 的 `./client` 才声明 `ctx.locale`；host 侧 `.` 导出只有 `apply(ctx)` 注册 settings 段 | shell-pages（85 条）、auth-gate（44 条）、Go 错误、portal 全都拿不到 `t()`——这是它们零覆盖的**根本原因**，不是懒 |
| **S2** | **槽位外挂载的树不会随语言重渲染** | `cron/src/client/panel-mount.tsx:92` 用独立 `createRoot` 把 cron 主区挂进会话列，在 renderer 的 `useLocaleRevision` 通道之外；只有 SSE 事件才触发重渲染 | cron 面板打开时切语言，标签停在旧语言直到有 cron 事件或刷新。memory-evolve 的命令式根（`notification-bell.tsx:788`、`bookmark-injector.tsx:289`）同类 |
| **S3** | **语言跟随失败时静默回落到中文** | 五个包都 `catch { /* keep the last known locale */ }`，而 `locales.ts` 初值是 `'zh'` | 上游一旦改名/移除 `getLocale`，英文用户静默看到中文，**零报错** |
| **S4** | **语言判断机制四套并存** | ① 注册字典 + `ctx.locale`（正确）② memory-evolve 用 `bind`（正确）③ `CoIView.tsx:479` / `PromptView.tsx:318` 私有字典 + `navigator.language` ④ 5 份手写 `isEn()` | 用户在设置里切语言，③④ 覆盖的界面**不跟随**（它们看的是操作系统语言） |
| **S5** | **原生语言只认 `zh`/`en` 两个字面量** | `desktop/src/index.ts:75-78` 的 `normalizeDesktopLocale` 丢弃其它值 | 语言包注册 `zh-CN`（`LOCALE_ID_PATTERN` 允许）时渲染进程用得上、托盘被丢弃 → 回落系统语言。另外渲染进程按 `navigator.languages` 有序列表匹配、托盘只看一次 `app.getLocale()`，多语言列表下**两者可能给出不同结果** |
| **S6** | **i18n 死键守卫覆盖率 4/6** | `desktop/tests/i18n-keys.spec.ts:19-24` 的 `DICTIONARY_PACKAGES` 只有 account-card / browser / connectors / cron | 漏了 **enterprise（171 键，最大字典）** 与 memory-evolve。**实测把 enterprise 加进去会红：79 个死键** |
| **S7** | **注册的字典目前近乎空转** | 五个包的组件都从 `./locales.ts` import `t`，无人消费框架的 `t` 席位（`PropsLocale` 只出现在注释里）；cron 是唯一声明 `locale: LOCALE_NS` 的 | 今天删掉 `register` 对渲染无影响——但 cron 那两个声明了 `locale:` 的条目会静默渲染原始 key。另：同名命名空间重复注册是**硬失败**（`locale:396-400` throw），不是合并 |
| **S8** | **CI 从不跑英文界面** | `scripts/e2e-client.mjs:221` 恒用 `--lang=zh-CN`，断言是约 48 行中文 marker | 上述所有英文路径缺陷对 E2E 全不可见 |

### 6.1 enterprise 字典的 79 个死键（46%）

`packages/host/enterprise/src/client/locales.ts` 171 键中 **79 键在代码里零引用**（zh/en 两侧都有，纯冗余）：

| 前缀 | 条数 | 来源 |
|---|---|---|
| `skill.*` | 36 | 旧的独立技能面板（已并入 CapabilityCenterPanel） |
| `agent.*` | 31 | 旧的独立 Agent 面板（同上） |
| `session.*` | 6 | auth-gate 接管之前的登录表单 |
| `capability.*` | 5 | `tabOrg` / `emptyOrg`（市场·组织合并后废弃）、`installing`、`uploading`、`versionCount` |
| `account.password.success` | 1 | 从未接线的成功提示 |

这不是本次新发现——`docs/AUDIT-2026-09-08-FULL.md:193` 已记录「零引用 i18n 键约 79 条」，其修复计划（`§13.3` P3「i18n 死键清理 26 条 + 断言」）**未完成**。由于 S6 的守卫缺口，没有门禁能拦住它。

---

## 7. 模型可见文案（B 类，需要产品拍板，不是遗漏）

这类是**发给 LLM 的工具描述 / 系统提示词**，host 侧拿不到 `ctx.locale`。总 72 条。真正的问题是**语言不一致**：

| 位置 | 现状 | 风格 |
|---|---|---|
| `browser/src/tools.ts`（36） | **英文正文 + 中文方括号前缀**：`[导航] Open the browser (…)`、`[交互]`、`[读取]`、`[记忆]`、`[控制]`、`[产物]`；英文句子里嵌 `我来操作`(×5)、`交给 AI`(×9)、`清除数据` | 中英混排（最不一致） |
| `cron/src/tools.ts`（28） | 纯中文描述 + 6 条 `output.render.text` 回执（`已创建定时任务 ${id}`）+ 7 条 throw | 统一中文 |
| `cron/src/index.ts:45` | `CRON_GUIDANCE` 系统提示纯中文 | 统一中文 |
| `browser/src/pool.ts`、`runtime.ts` | 用户闸拒绝/超时消息中英混杂 | 混排 |

补充事实：`browser/tools.ts` 里的 `[导航]` 等标签**无功能作用**——真正的分组策略在 `tools.ts:1264` 的 `GROUP_OF`，用的是英文标识（`navigate`/`interact`/`read`/…）。所以这些中文前缀是纯装饰，可安全本地化或统一。

**建议**：明确二选一并落进 owning Agent Note。当前 `browser` 与 `cron` 各选了一种，下一个 `tools.ts` 会选第三种。

---

## 8. 修复顺序与工作量

### P0（英文用户的「能不能用」问题）

| # | 目标 | 条数 | 前置 |
|---|---|---|---|
| 1 | **webadmin 弹窗与破坏性确认** | 29 模态 + 17 原生 | 先建本地化 `ConfirmDialog` + `LocaleProvider`；原生 `confirm` 无法就地翻译 |
| 2 | **登录 / 改密 / 恢复页**（`auth-gate.ts`） | ~44 | 需决定：host 注入 locale，或页面内 `navigator.language` 分支（预认证场景后者更省） |
| 3 | **内置浏览器两页**（`shell-pages.ts`） | ~85 | 需 host 侧 locale 通道（S1）；browser 包已有 Electron adapter，是自然接缝 |
| 4 | **memory-evolve 画板 6 弹窗** | 58 | **`t` 已在 `CanvasView` props 上**，只需往下传 3 层 + 4 个调用点 |
| 5 | **memory-evolve Advisor 面板** | 131 | **`t` 已传入**；但需先拍板「面板主体固定中文」这条旧决定是否撤销 |

### P1（高频可见）

| # | 目标 | 条数 |
|---|---|---|
| 6 | webadmin 外壳与登录（`App.tsx`/`nav.ts`/`rbac.ts`/`Login.tsx`/`password-dialog`/`mfa-settings-dialog`） | ~90 |
| 7 | webadmin 高频页（`Users`/`Gateway`/`Departments`/`usage/*`） | ~600 |
| 8 | **决定 Go 错误策略**：`Accept-Language` 协商 vs 错误信封加稳定 `message_key` | 722 条消息 |
| 9 | 门户页 + `lang` | 整页 |
| 10 | enterprise 上传预检 23 + 面板 1 + hero tagline 1（+ CSS 兜底去重） | 25 |
| 11 | desktop：Linux 更新对话框 2 + 崩溃页 1 + 更新徽标 1（顺带给 desktop client 加 `locale` 注入） | 4 |
| 12 | branding「关于」段落（加 `locale` 注入，或并入 enterprise 字典） | 1 |

### P2 / 硬化（与上面同批做，别拖后）

| # | 目标 | 说明 |
|---|---|---|
| 13 | **`DICTIONARY_PACKAGES` 补 enterprise + memory-evolve** | **一行改动追补 171 键的守卫**；补之前先清 79 死键 |
| 14 | 加「`en` 列不得含 CJK」守卫 | 能拦住 browser 那处残留 |
| 15 | 给 browser 的 `t()` 补 `params` 签名 | 与其余四包对齐，才能修 `{button}` |
| 16 | 收编 4 套语言判断到 `ctx.locale`（S4） | 涉及 `CoIView`/`PromptView` 的私有字典（297 条）与 5 份 `isEn()` |
| 17 | 修槽位外挂载树的重渲染（S2） | cron panel-mount + memory-evolve 两个命令式根 |
| 18 | E2E 补一条英文语言断言（S8） | 否则上述缺陷永远不可见 |
| 19 | 明确 B 类文案策略（§7） | 一句话决策，落 Agent Note |
| 20 | 清 enterprise 79 死键 + memory-evolve 12 条死种子常量 | 见 §6.1；后者 `createSeedState ← loadCanvasState` 零调用者 |

### 工作量粗估

- **webadmin 客户端侧**：约 45 个文件、~1,300 键、23 个测试文件迁移（631 处）⇒ **约 2 周/1 人**；若把 Go 错误面纳入 **再 +1.5~3 周**
- **memory-evolve**：~313 条、约 580 个新键（canvas ~151 / advisor ~131 / coi ~181 / prompt ~119）⇒ **约 1 周**
- **预认证页 + 浏览器窗口 + desktop/branding 零散项**：~135 条 ⇒ **约 3~5 天**（主要成本是 S1 的 host 侧 locale 通道）

---

## 9. 已验证的「非缺陷」（勿重复报告）

审计中排除的假阳性，记录以免下轮重复劳动：

- **`memory-evolve` 注册字典**：795/795 键完全对齐，`en` 列零 CJK 残留，零空值 ⇒ **健康**。问题只是覆盖（advisor 仅 2 键、canvas 0 键、coi/prompt 0 键）
- **`MemoryQueueView.tsx`、`BroadcastView.tsx`、`SyncView.tsx`、`TodoView.tsx`、`BookmarksView.tsx`、`bookmark-injector.tsx`、`notification-bell.tsx`**：全部 `isEn()` 双语或匹配/解析模式，**非缺口**
- **匹配/解析用途的中文**（非展示）：`notification-bell.tsx:247-251`（同时接受 `主题`/`Subject`，是**好实践**）、`bookmark-injector.tsx:41` `BRANCH_PATTERNS`、`BookmarksView.tsx:75,104,108`（匹配上游 tab 文案）、`friendly-error.ts` 5 条 `raw.includes('…')`、`connectors/src/index.ts:1263`、`AccountSection.tsx:153` `raw.includes('原密码')`、`browser/src/store.ts:317` 全角 `＝` 分隔符
- **`memory-evolve/canvas-grok/constants.ts` 的 11 条死种子字符串**：`createSeedState ← loadCanvasState` 零调用者
- **`desktop/src/tray-locale.ts`（14）**：本身就是 zh/en 双语表，**是 i18n 实现不是缺口**
- **`desktop/src/loop-notify.ts`**：已通过 `runtime.locale` → `COPY[locale()]` 正确本地化，**已做对**
- **`electron-runtime.ts:262`** 目录选择器标题：已用 `currentLocale` 三元本地化
- **`desktop/scripts/**`（299 条）**：打包/E2E 工具，`package.json` 的 `files` 已排除，不进产物
- **只含注释的文件**：`SkillsBrowser`(1005 汉字)、`MemoryTabView`(1969)、`ModelsTabView`、`VersionTabView`、`TurnBookmarkButton`、`SkillsTabView`、`TodosTabView`、`canvas-grok/types.ts`、`TabGuideView`、`SettingsTabView`、`UiSettingsView`、`AliasButton`、`CopySessionIdButton`、`HeaderActions`、`wide-chat`、`context-meter-warn`、`ui-settings-features`、`canvas-grok/store.ts`、`skills-browser/contract.ts`
- **CSS**：全仓无任何 `content:` 声明携带 CJK；CSS 里的中文全是注释
- **`packages/client/account-card`**：**零缺口**——所有中文都在 `locales.ts` 里。**这是参考实现**

---

## 10. 方法学备注

1. **grep 的行数 ≠ 字符串数。** 一个整页 HTML 是**一个**字符串字面量节点却含几十条文案：`shell-pages.ts` 85 条文案只占 2 个节点，`auth-gate.ts` 44 条只占 12 个节点。反之 grep 计数会把注释算进去（`loop-notify.ts` 9 处「中文」全是注释、`SkillsBrowser` 1005 个汉字零字面量）。**必须用 AST + 逐消费点判定。**
2. **模板字面量里的 `${…}` 表达式文本不能重复计数**（首轮扫描踩过，会虚高）。
3. **`import type {} from '…/client'` 的上游代码里也有中文**——审计只统计本仓自研代码。
4. **复现命令**（本次产出的证据脚本，`temp/` 已 gitignore）：
   - `node temp/i18n-audit/astcjk.mjs <pkg…>` — AST 扫硬编码 CJK 字面量
   - `node temp/i18n-audit/jsxscan.mjs <pkg…>` — AST 扫未本地化 JSX 文本/属性
   - `node temp/i18n-audit/enleak.mjs` — 字典键对齐 + `en` 列 CJK 残留
   - `node temp/i18n-audit/guardcheck.mjs` — 死键守卫覆盖率与假设补全后的结果
   - `node temp/i18n-audit/deadkeys.mjs` — enterprise 死键按前缀分组
   - `node temp/i18n-audit/me-dict.mjs` — memory-evolve 字典健康度
5. **守卫确实在 CI 里跑**（一处子审计的表述需更正）：`scripts/check-workspaces.mjs:60` 阶段 1 跑 `dsh-plugin-desktop check`，含 `vitest run`，而 `packages/host/desktop/vitest.config.ts` 的 `include: ['tests/**/*.spec.ts']` 覆盖 `tests/i18n-keys.spec.ts`。**守卫在跑，只是只覆盖 4 个包。**

---

## 11. 修复记录（2026-09-16，本轮实施完毕）

**范围**：客户端全部缺口（服务端 webadmin / Go 错误文案 / 门户页按用户要求不在范围内）。
**门禁**：`corepack yarn check` → **16/16 任务通过、0 失败、0 跳过，exit 0**（含 8 个包 + 8 个守卫脚本）。

### 11.1 覆盖面变化

用同一套 AST 扫描器（`temp/i18n-audit/astcjk.mjs`）在改动前后各跑一次：

| 包 | 改动前 | 改动后 | 残留说明 |
|---|---|---|---|
| `vendor/memory-evolve` | 281 | 39 | 真实非文案 21 条（含搜索词表 6、过滤哨兵 13、mermaid 括号转写 2）；其余是字典块与 `hostCopy` 对的 zh 侧 |
| `host/connectors` | 119 | 26 | 12 条 `ctx.logger` 内部行 + 15 条「流程步骤名」（语言无关的键，经 `stepLabel()` 渲染） |
| `host/enterprise` | 53 | 45 | 预认证页/预检文案的 `{zh,en}` 表 zh 侧 + 刻意保留的 `原密码` 匹配器 + 注释 |
| `host/browser` | 63 | 25 | `store.ts` 内部告警 13（唯一去向 `options.warn ?? console.error`）+ 注入探针脚本代码 1 + 两页文案表 zh 侧 |
| `host/cron` | 29 | 16 | 15 条工具**参数**描述 + 1 条工具描述（模型面，按策略保留中文，见 §11.4） |
| `host/desktop` | 6 | 2 | 1 条 CSS 注释 + `选择工作区目录`（本身就在 `currentLocale === 'zh' ? … : …` 三元里，扫描器误报） |
| `client/branding` | 1 | **0** | — |
| `client/account-card` | 0 | 0 | 本来就是参考实现 |

**注意**：扫描器统计的是「含中文的字符串字面量」，因此**必然**把双语表与 `hostCopy(locale, zh, en)` 的 zh 参数计入。153 这个数字**不等于**剩余缺口数——它主要是双语实现的正常形态。逐条分类见各包的提交说明与新增测试。

### 11.2 各包交付

**memory-evolve（最大单块）**
- 字典 **797 → 1417 键（+620）**，zh/en 键集完全一致（`onlyZh=0 onlyEn=0`），`en` 列零中文残留。
- `canvas-grok` 全子树接上 `t`（`CanvasView` 此前**声明了 `t` 却从未读取**）；Tab 名在 `label` 回调内求值，切语言即刻跟随。
- **`TYPE_LABEL` → `TYPE_LABEL_KEYS` + `typeLabel(type, t)`**，并新增语言无关的 `TYPE_SEARCH_TERMS` 保住搜索语义（有测试证明「文件夹/folder」都能命中、切语言不改搜索结果）。
- **`LEVEL_LABEL` → `LEVEL_KEYS` + `levelLabel(level, t)`**；store 自产文案改为**注入 `t`**（含模板与错误兜底，不能只存键）。
- `CoIView` / `PromptView` 的**私有双语字典整体迁入注册字典**（299 条），`~18` 处手写 `isEn()`（读 OS 语言、无视应用内设置）全部删除。
- 顺带发现并修掉清单外 2 个真缺口：`mermaid-render.ts`（自己按 `document.documentElement.lang` 判语言）、`VersionTabView.tsx`（3 处全角冒号）。
- **死代码选择删除而非翻译**：`createSeedNodes`/`createSeedState`/`loadCanvasState`（零调用点）。

**预认证页面（enterprise）**
- 登录 / 强制改密 / 恢复中 / 返回按钮 → 4 个 `HostLocale` 函数 + `{zh,en}` 文案表（27/12/1/1 条），`<html lang>` 正确。
- 语言**按请求解析**（路由处理器）与**按渲染解析**（`tapIndex`），无模块级冻结；有测试证明同一实例先 zh 后 en 且无需重新 apply。
- 顺带修掉同类 8 处 `归档过大（超过 16MB）`。
- **zh 输出与改动前逐字节一致**（三页 diff 为空，仅内联脚本由 `'文案'` 变为 `T.*`）。

**内置浏览器（browser）**
- 两页从模块常量变为 `browserShellHtml(locale)` / `browserOverlayHtml(locale)`，**87 条**文案（工具栏/空态/toast/控制权按钮/29 条动作时间线/菜单/三个查看器/软键盘），`<html lang>` 正确，内联脚本经 `\u003c` 转义（翻译内容无法闭合 `<script>`）。
- 窗口标题、ops 时间线、用户闸拒绝/超时消息改为按调用取 locale；修掉 `open the 浏览器 window` 中英混排。

**连接器 + 定时任务（connectors / cron）**
- connectors 新增 `src/host-copy.ts`（91 键）承载宿主文案；OAuth 回环回调页（用户浏览器里渲染的 HTML）本地化。
- **`friendly-error` 耦合正面解决**：新增 `src/connector-error.ts` 定义语言无关的**错误码**（`auth-required`/`exit-code`/`command-missing`/`download`）+ 可选 `errorCode` 字段（加法式），客户端不再靠中文字符串匹配；旧的意外子串语义被显式编码（`AUTHORIZING_STEPS`），行为在 zh 下逐字节不变。
- cron：转录区回执与工具抛错本地化；`CRON_GUIDANCE` 改为**惰性 thunk**，每次提示词组装时按当前语言渲染。

**desktop / branding**
- 新增 `packages/host/desktop/src/host-locale.ts`（`dsh-plugin-desktop/host-locale`）——**宿主侧唯一的语言来源**（探测 `desktopRuntime.locale` → 请求 `Accept-Language` → 产品默认），插件不再各自发明。
- 原生更新对话框（整段含按钮，此前只有 Linux 的 detail 是中文、其余恒英文 = 中英混排）与崩溃兜底页（含 `lang`）本地化。
- desktop 客户端补 `locale` 注入 + `client/locales.ts`：更新徽标 `安装 X` / `Install X` 不再与英文 `title` 混排。
- branding「关于」段中英混排修复（`brand-shell.tsx` + 新增 `locales.ts`）。

### 11.3 守卫（防止再次退化）

| 守卫 | 内容 |
|---|---|
| `tests/i18n-dictionary-hygiene.spec.ts`（**新增**） | **自动发现**全仓字典（不再靠白名单），逐份断言：键集镜像 / `en` 列无中文 / 无空值 / 两侧都有键。发现数被断言（防「一个也没找到」的假绿）。当前 8 份字典 × 4 条 = 33 用例 |
| `tests/i18n-keys.spec.ts`（扩容） | 白名单 **4 → 7 个包**（补入 enterprise —— 它是最大的字典却从未被覆盖，79 个死键就是这么留下的；另补 branding、desktop） |
| `tests/electron-languages.spec.ts`（他人落地） | 两个平台的资源基名都断言被 `electronLanguages` 命中 |
| 各包新增 | `auth-gate-i18n.spec.ts`(12) · `host-i18n.spec.ts` · `locales-hygiene.spec.ts` · `shell-pages-locale.spec.ts`(11) · `host-copy.spec.ts` ×2 · `canvas-type-label-i18n.test.js`(7) · `advisor-i18n-structure.test.js`(5) · `client-i18n-coverage.test.js`(2) · `host-locale.spec.ts`(13) |
| 死键清理 | enterprise 删除 **79 个死键**（`skill.*` 36 / `agent.*` 31 / `session.*` 6 / `capability.*` 5 / `account.password.success` 1），字典 171 → 94 |

其中「冻结语言」这一类回归是**变异验证**过的：临时把 locale 固定在 apply 期（connectors）或注册期（cron），分别有 3 条与 4 条测试变红。

### 11.4 明确不做 / 保留

- **cron 工具描述保留中文**（4 条工具 + 15 条参数）：模型面契约，按用户「服务端不用支持多语言」的同一精神不纳入本轮；转录区回执已本地化。
- **browser 工具描述改为纯英文**：描述在 apply 期注册一次、无法跟随请求语言，只能选一种；原先是「英文正文 + 中文 `[导航]` 装饰前缀」的混排。中文前缀本无功能作用（分组真源是 `GROUP_OF` 的英文 id），现改为与之一致。**这是一处产品决策，需要时容易翻转。**
- **内部诊断不翻**：`ctx.logger` 行、`store.ts` 的 `warn/degrade`、`console.warn` 解析告警。
- **必须保持双语的匹配/解析模式**（已加测试钉住，防止下一轮「全量翻译」改坏）：`notification-bell` 邮件头解析、`BookmarksView` 上游 tab 文本匹配、`bookmark-injector` 分支模式、`TYPE_SEARCH_TERMS`、PromptView 过滤哨兵、mermaid 括号转写表。
- **服务端（Go）文案仍为中文**：webadmin、门户页、API 错误信封（722 条）按用户要求不在本轮；`AccountSection` 的 `原密码` 嗅探因此保留（已加注释说明耦合与风险）。
- **未在真机 Electron 里点过**：memory-evolve 无 React 渲染测试设施，其 UI 行为只验证到「源码结构 + 打包产物」级；浏览器/enterprise 的页面有 jsdom 行为测试但只跑 zh 变体。

### 11.5 渠道多语言

结论与落地方案见 `docs/planning/2026-09-16-channel-locale-copy.md`：**需要，但只覆盖「散文类」字段**（`login_tagline` / `client_tagline` / `login_welcome` / `portal_welcome`），品牌名不翻译；另建议增加可选 `defaults.locale`。其中「英文登录页显示中文标语」是**当前就存在的可见缺陷**（`official`/`beta` 都配了中文 tagline，而登录页会渲染它）。
